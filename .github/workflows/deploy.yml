name: Deploy and publish Extension

on:
  release:
    types: [created] # TODO: on publish instead

# https://github.com/marketplace/actions/publish-vs-code-extension
# https://code.visualstudio.com/api/working-with-extensions/continuous-integration
jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit # for GH_PAT

  deploy:
    permissions: write-all
    needs: build
    runs-on: ubuntu-latest

    # Handle secrets: https://docs.github.com/fr/actions/security-guides/using-secrets-in-github-actions
    # Get tokens: githuhttps://code.visualstudio.com/api/working-with-extensions/publishing-extension#get-a-personal-access-token
    steps:
      - uses: actions/checkout@v4
      # TODO: Should instead use the value of published data
      - name: Download VSIX
        uses: actions/download-artifact@v4
        with:
          name: extension
      #- name: Publish to Open VSX Registry
      #  uses: HaaLeo/publish-vscode-extension@v1
      #  id: publishToOpenVSX
      #  with:
      #    pat: ${{ secrets.OPEN_VSX_TOKEN }}
      #    extensionFile: shader-validator.vsix # TODO: version in path
      #- name: Publish to Visual Studio Marketplace
      #  uses: HaaLeo/publish-vscode-extension@v1
      #  with:
      #    pat: ${{ secrets.VS_MARKETPLACE_TOKEN }}
      #    registryUrl: https://marketplace.visualstudio.com
      #    extensionFile: ${{ steps.publishToOpenVSX.outputs.vsixPath }}
      - name: Upload assets to release
        run: gh release upload ${{ github.ref_name }} shader-validator-0.0.1.vsix # TODO: version in path should be retrieved by 
        env:
          GH_TOKEN: ${{ github.token }}